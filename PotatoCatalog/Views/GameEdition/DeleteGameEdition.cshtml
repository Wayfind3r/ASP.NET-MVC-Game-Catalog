@model PotatoCatalog.Models.GameEditionViewModel

<h5 class="col-md-offset-1">Are you sure you want to delete this?</h5>
<div>
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.EditionTitle)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.EditionTitle)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.PriceInPotatoes)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.PriceInPotatoes)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.Contents)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Contents)
        </dd>
    </dl>

    @using (Html.BeginForm("DeleteGameEdition", "GameEdition", new { gameId = Model.GameId, editionId = Model.Id }, FormMethod.Post))
    {
        @Html.AntiForgeryToken()

        <div class="modal-footer">
            <button type="button" class="btn btn-default" data-dismiss="modal">Close</button>
            <input type="submit" value="Delete" class="btn btn-primary" />
        </div>
    }
</div>
